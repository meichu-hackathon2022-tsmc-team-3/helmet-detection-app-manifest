apiVersion: v1
kind: Service
metadata:
  name: event-handler-svc
  namespace: {{ .Values.app.namespace }}
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 5280
    targetPort: 5280
  selector:
    app: event-handler